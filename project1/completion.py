import random
import numpy as np


def complete(img, size, mode="random"):
    """
    :param img: the src texture
    :param size: size of result
    :param mode: mode of patch placement
    :return: the completed image
    """
    completed_img = np.zeros(img.shape)
    if mode == "random":

    return completed_img